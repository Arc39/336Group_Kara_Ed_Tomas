create table eyemakeup (
      id INT NOT NULL AUTO INCREMENT,
      brand varchar(55),
      itemName varchar(55),
      price numeric(5,2),
      PRIMARY KEY(id)
      
);

create table facemakeup (
      id INT NOT NULL AUTO INCREMENT,
      brand varchar(55),
      itemName varchar(55),
      price numeric(5,2),
      PRIMARY KEY(id)
      
);

create table skincare (
      id INT NOT NULL AUTO INCREMENT,
      brand varchar(55),
      itemName varchar(55),
      price numeric(5,2),
      PRIMARY KEY(id)
      
);