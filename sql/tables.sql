create table eyemakeup (
      id INT,
      brand varchar(55),
      itemName varchar(55),
      price numeric(5,2),
      PRIMARY KEY(id)
      
);

create table facemakeup (
      id INT,
      brand varchar(55),
      itemName varchar(55),
      price numeric(5,2),
      PRIMARY KEY(id)
      
);

create table skincare (
      id INT,
      brand varchar(55),
      itemName varchar(55),
      price numeric(5,2),
      PRIMARY KEY(id)
      
);